# Ver README.md — cada app tem seu próprio .env.example com o subconjunto
# de variáveis que ele usa (apps/mobile, apps/backend, etl).
#
# Resumo de todas as variáveis usadas no projeto:

# --- apps/mobile ---
EXPO_PUBLIC_API_URL=
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=

# --- apps/backend ---
DATABASE_URL=
SUPABASE_SERVICE_KEY=
MINDEE_API_KEY=
RECEIPT_OCR_PROVIDER=mock
PLUGGY_CLIENT_ID=
PLUGGY_CLIENT_SECRET=
BANK_AGGREGATOR_PROVIDER=mock

# --- compartilhado (backend + etl) ---
SUPABASE_URL=
